#!/usr/bin/env node

const program = require('commander');

program
    .version(require('../package').version)
    .usage('<command> [options]')
    .command('list', 'List available templates')
    .command('init', 'Init a new project in the current folder, from git')
    .command('generate', 'Generates new code')
    .parse(process.argv);
